#!/usr/bin/env bash
<% 
	users = @users
    firstUser = users.shift
 -%>
echo <%= firstUser[1] %> | htpasswd -ic <%= @httpPasswdPath %> <%= firstUser[0] %>
<% users.each do |key, value| -%>
echo <%= value %> | htpasswd -i <%= @httpPasswdPath %> <%= key %>
<% 	end -%>